{
  "scripts": {
    "build": "yarn clean && tsc",
    "test": "jest --watchAll",
    "test:cov": "jest --coverage",
    "clean": "rm -rf coverage build *.log"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.3"
  }
}
